package Ex_13_ExceptionHandling.Lab101_TryCatchFinally;

import java.io.FileInputStream;

public class CheckedExceptionExample {
    public static void main() {
        int a = 10;

        //FileInputStream fis = new FileInputStream("C://abc.txt");

        /*
        If  the named file does not exist, is a directory rather than a regular file,
        or for some other reason cannot be opened for reading then
        a FileNotFoundException is thrown.

        JVM recognizes the risk before running the program
        hence referred as one of the CheckedException
         */
    }
}
